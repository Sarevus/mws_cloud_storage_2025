# Server Configuration
server.port=6969

# Multipart configuration
spring.servlet.multipart.max-file-size=100MB
spring.servlet.multipart.max-request-size=100MB
spring.servlet.multipart.enabled=true

# Tomcat connection limits
server.tomcat.max-swallow-size=100MB
server.tomcat.max-http-form-post-size=100MB

# Connection timeout (120 seconds)
server.tomcat.connection-timeout=120000

# Datasource Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/cloud3_data
spring.datasource.username=developer
spring.datasource.password=dev123
spring.datasource.driver-class-name=org.postgresql.Driver

# Connection pool settings
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.connection-timeout=20000

# JPA Configuration
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.open-in-view=false

# Advanced Hibernate settings
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false

# Logging
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
logging.level.com.MWS=DEBUG


spring.web.resources.static-locations=classpath:/static/

spring.flyway.baseline-on-migrate=true
spring.flyway.validate-on-migrate=false
#
#spring.sql.init.mode=always
#spring.sql.init.schema-locations=classpath:view.sql


spring.mail.host=smtp.yandex.ru
spring.mail.port=465
spring.mail.username=CloudStorageMIPT@yandex.com
spring.mail.password=dbtvagktncyieszs
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true